# Direct runtime dependencies for legacy pip-based installs. Docker builds
# resolve [project] with uv.lock instead. Keep this list in sync with [project]
# in pyproject.toml. Test dependencies live in the `test` extra and are
# installed only by the CI workflow.

# Runtime
boto3
botocore
click
datacube
fiona
fsspec
geopandas
matplotlib
numpy
pandas
psycopg2-binary  # datacube's PostgreSQL driver; not imported directly
pyarrow
python-geohash
rasterio
rioxarray
s3fs
seaborn
shapely
SQLAlchemy
tqdm
xarray
