# Direct dependencies only. Transitive packages are pinned in constraints.txt,
# which the Dockerfile passes as `pip install -r requirements.txt -c constraints.txt`.
# Keep this list in sync with [project] in pyproject.toml.

# Runtime
boto3
botocore
click
datacube
dea-tools>=0.4.10
fiona
fsspec
geopandas
numpy
pandas
psycopg2-binary  # datacube's PostgreSQL driver; not imported directly
pyarrow
python-geohash
rasterio
rioxarray
s3fs
shapely
SQLAlchemy
tqdm
xarray

# Test (the CI image runs the suite with `coverage run -m pytest`)
coverage
moto
pytest
